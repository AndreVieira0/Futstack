{
  "name": "futstack",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace do Futstack com frontend React/Vite e API NestJS.",
  "scripts": {
    "dev:front": "npm run dev --prefix front-end",
    "dev:api": "npm run start:dev --prefix api-futstack",
    "build:front": "npm run build --prefix front-end",
    "build:api": "npm run build --prefix api-futstack",
    "lint:front": "npm run lint --prefix front-end",
    "lint:api": "npm run lint --prefix api-futstack",
    "test:api": "npm run test --prefix api-futstack"
  }
}
